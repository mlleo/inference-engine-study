# LLM 추론 엔진 튜토리얼 시리즈

## 제목
**LLM 추론 엔진 마스터하기: SGLang을 활용한 기초부터 프로덕션까지**

## 부제
LLM 추론 시스템 구축 및 최적화를 위한 4주 실습 스터디 프로그램

---

## 소개

대규모 언어 모델(LLM)은 현대 AI 애플리케이션의 핵심이 되었지만, 규모에 맞게 *어떻게* 효율적으로 실행되는지는 대부분의 실무자에게 여전히 미지의 영역입니다. 이 튜토리얼 시리즈는 높은 처리량(high-throughput)과 짧은 지연 시간(low-latency)으로 LLM을 서비스하기 위해 설계된 최첨단 추론 프레임워크인 **SGLang**의 아키텍처, 최적화 기법 및 구현 세부 사항을 살펴봄으로써 LLM 추론 엔진에 대한 궁금증을 해소합니다.

추론을 블랙박스로 취급하는 대신, 토큰 배치 처리(token batching), KV 캐시 관리, 어텐션 최적화 및 메모리 스케줄링과 같이 추론 속도를 높이는 실제 메커니즘을 학습하게 됩니다. 8개의 세션이 끝날 무렵에는 이러한 시스템을 깊이 이해할 뿐만 아니라, 여러분의 숙련도를 증명할 수 있는 실용적인 사이드 프로젝트를 구축하게 될 것입니다.

이 과정은 예산 부담이 적은 실습 중심의 학습입니다. RunPod(테스트 세션당 $10-15)을 사용하여 실제 GPU에서 실험하고, 실제 추론 워크로드를 실행하며, 성능 향상을 직접 측정해 봅니다. 완료되면 인스턴스를 종료하고 다음 개념으로 넘어가면 됩니다.

---

## 학습 목표

이 4주 프로그램을 완료하면 다음을 달성할 수 있습니다:

1. **전체 추론 파이프라인 이해** — 모델 로딩부터 토큰 생성, 출력 포맷팅까지
2. **SGLang 아키텍처 마스터** — vLLM, Ollama, TensorRT-LLM과 같은 대안과 어떻게 다른지 파악
3. **추론 성능 최적화** — 배치 처리, 추측 해독(speculative decoding), KV 캐시 관리 등의 기법 적용
4. **디버깅 및 벤치마킹** — 추론 워크로드 프로파일링, 병목 현상 식별 및 개선 사항 측정
5. **프로덕션 레벨 시스템 구축** — 실제 추론 문제(예: 특화된 서빙 시스템, 벤치마킹 도구 또는 사용자 정의 추론 최적화)를 해결하는 사이드 프로젝트 생성
6. **결과 발표** — 숙련도의 증거로 사이드 프로젝트를 문서화하고 발표

---

## 세션별 세부 내용

### **1주차: 기초**

#### **세션 1: LLM 추론 기초**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- 추론이란 무엇인가? (프롬프트 단계 vs 생성 단계)
- 트랜스포머 아키텍처 복습: 임베딩, 어텐션, 피드포워드, 출력 프로젝션
- 추론이 학습(training)과 다른 이유: 단일 토큰 생성 루프, 메모리 제약, 지연 시간 민감도
- 추론의 병목 현상: 연산 중심(compute-bound) vs 메모리 중심(memory-bound) 작업
- 주요 지표: 처리량(tokens/sec), 지연 시간(첫 토큰 도달 시간, 토큰 간 지연 시간), 메모리 사용량

**실습 활동:**
- RunPod에서 Hugging Face transformers를 사용하여 소형 LLM(예: Mistral-7B 또는 Phi-2) 로드
- 간단한 Python 루프로 토큰을 생성하고 지연 시간 측정
- `torch.cuda.memory_summary()`를 사용한 메모리 사용량 프로파일링
- 배치 크기가 생성 속도에 미치는 영향 관찰
- **결과물:** 텍스트를 생성하고 타이밍/메모리 지표를 기록하는 간단한 Python 스크립트

**필요 리소스:**
- GPU(A40, RTX4090 또는 L40)가 탑재된 RunPod 인스턴스 — 시간당 약 $0.40-0.65
- 15-30분의 실행 시간

---

#### **세션 2: SGLang 아키텍처 개요**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- SGLang 설계 철학: 기수 어텐션(radix attention), 효율적인 배치 처리, 구조화된 생성(structured generation)
- SGLang과 vLLM의 차이점: 목표는 비슷하지만 내부 구조가 다름
- SGLang의 핵심 구성 요소:
  - TokenizerManager
  - RequestManager 및 스케줄러
  - Model runner 및 GPU 메모리 풀
  - RadixAttention 구현
- SGLang 설치 및 첫걸음
- 지원되는 모델 및 양자화 전략 (GPTQ, AWQ, bfloat16, int8)

**실습 활동:**
- RunPod에 SGLang 설치 (소스에서 빌드하거나 사전 빌드된 wheel 사용)
- 기본 추론 서버 실행: `python -m sglang.launch_server --model-path mistralai/Mistral-7B-Instruct-v0.2`
- REST API를 통한 요청 전송 (간단한 curl 명령 및 Python 클라이언트)
- 세션 1의 단순 접근 방식과 지연 시간/처리량 비교
- 여러 요청을 생성하고 배치 처리 동작 관찰
- **결과물:** 문서화된 설정 가이드 + 단순 방식과 SGLang 추론을 비교한 벤치마크

**필요 리소스:**
- RunPod 인스턴스 (세션 1과 동일)
- 20-40분의 실행 시간

---

### **2주차: 핵심 메커니즘**

#### **세션 3: 토큰 생성 및 샘플링 전략**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- 생성 루프: 순전파(forward pass) → 로짓(logits) → 샘플링 → 다음 토큰
- 샘플링 전략: 그리디(greedy), 온도(temperature), top-k, top-p(nucleus), 빔 서치(beam search)
- 샘플링이 중요한 이유: 다양성 vs 결정론, 지연 시간에 미치는 영향
- 스트리밍 생성: 토큰이 생성되는 즉시 출력하는 방법
- 배치 생성: 병렬로 여러 시퀀스 생성
- SGLang의 StopStrings 및 구조화된 제약 조건

**실습 활동:**
- 동일한 프롬프트에 대해 다양한 샘플링 전략을 비교하는 스크립트 작성:
  - 그리디 디코딩
  - Top-k=40, top-p=0.95
  - 온도 변화 (0.5, 1.0, 2.0)
  - 빔 서치 (시간이 허락할 경우)
- 각 샘플링 방법의 지연 시간 측정
- SGLang의 제약 조건 시스템 실험 (예: `gen_kwargs={"max_new_tokens": 100, "temperature": 0.7}`)
- 생성된 토큰을 실시간으로 스트리밍하고 토큰 간 지연 시간 관찰
- **결과물:** 비교 분석 스크립트 + 각 샘플링 방법에 대한 지연 시간 측정 결과

**필요 리소스:**
- RunPod 인스턴스
- 20-30분의 실행 시간

---

#### **세션 4: 배치 처리 및 요청 스케줄링**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- 배치 처리가 중요한 이유: 상각된 연산(amortized computation), GPU 활용도
- 배치 구성의 과제: 가변적인 시퀀스 길이, 가변적인 생성 길이
- 패딩 및 마스킹: 모델이 가변 길이 배치를 처리하는 방법
- SGLang의 RadixAttention: 배치 간 KV 캐시 재사용을 최적화하는 방법
- 스케줄러 설계: FCFS vs 우선순위 큐 vs 적응형 스케줄링
- 토큰 예산 및 지연 시간 격리: 처리량과 공정성 간의 트레이드오프
- 요청 대기열: 백프레셔(backpressure) 및 거절 처리

**실습 활동:**
- SGLang에 10개의 동시 요청을 보내고 다음을 관찰:
  - 요청이 어떻게 함께 배치 처리되는가
  - 배치가 커짐에 따른 GPU 메모리 할당
  - 배치 내 초기 요청과 후기 요청의 지연 시간 차이
- 배치 크기를 변경하며 처리량(tokens/sec) 측정:
  - 배치 크기 1, 4, 8, 16, 32 (메모리 부족 시 중단)
  - 배치 크기 대비 처리량 그래프 작성
- 배치 크기 대비 토큰 간 지연 시간 측정:
  - 지연 시간이 배치 멤버 간에 일관된가, 아니면 증가하는가?
- 다양한 배치 타임아웃 설정 실험:
  - 공격적인 배치 처리(더 오래 대기) vs 조기 디스패치
- **결과물:** 배치 크기 분석 스크립트 + 처리량 및 지연 시간 곡선

**필요 리소스:**
- RunPod 인스턴스
- 30-40분의 실행 시간
- 부하 테스트 도구 (예: Python `asyncio` 또는 `locust`)

---

### **3주차: 최적화**

#### **세션 5: 어텐션 메커니즘과 메모리 관리**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- 멀티 헤드 어텐션: 작동 원리와 계산 비용이 높은 이유
- 셀프 어텐션 연산: 시퀀스 길이에 따른 O(n²) 복잡도
- KV 캐시: 키/값 행렬을 캐싱하여 메모리를 연산과 교환
- KV 캐시 증가: 시퀀스가 길어질수록 메모리가 폭발적으로 증가하는 이유
- 메모리 레이아웃: GPU 활용을 위한 최적의 텐서 형태
- 어텐션 최적화 기법:
  - Flash Attention (빠르고 메모리 효율적인 어텐션)
  - PagedAttention (단편화된 KV 캐시에 대한 vLLM의 접근 방식)
  - RadixAttention (SGLang의 트리 기반 KV 캐시 공유)

**실습 활동:**
- 다양한 시퀀스 길이로 추론 중 GPU 메모리 모니터링:
  - 짧은 문맥 (예: 512 토큰)
  - 긴 문맥 (예: 4096 토큰)
  - 매우 긴 문맥 (예: 32768 토큰, 모델이 지원하는 경우)
- 시퀀스 길이 대비 메모리 사용량 그래프 생성
- 어텐션 연산 시간 프로파일링:
  - PyTorch 프로파일러 또는 SGLang의 내장 프로파일링 사용
  - 어텐션 vs FFN 레이어에 소요되는 시간 비율 측정
- Flash Attention 활성화 vs 비활성화 상태에서 SGLang 테스트:
  - 처리량 및 지연 시간 비교
  - 메모리 사용량 비교
- SGLang의 문맥 캐싱(context caching) 실험:
  - 이전 요청의 KV 캐시 재사용
  - 캐시 적중률 및 속도 향상 측정
- **결과물:** 메모리 프로파일링 스크립트 + 어텐션 성능 분석

**필요 리소스:**
- 충분한 VRAM이 있는 RunPod 인스턴스 (긴 문맥의 경우 16GB 이상)
- 30-40분의 실행 시간
- PyTorch 프로파일러 도구

---

#### **세션 6: KV 캐시 최적화 및 추론 효율성**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- KV 캐시 수명 주기: 할당 → 채우기 → 재사용 → 퇴거(eviction)
- 접두사 공유(Prefix sharing): 여러 요청이 동일한 프롬프트 접두사를 공유할 때
- 추측 해독(Speculative decoding): 초안 모델이 후보를 생성하고, 검증 모델이 이를 확인
  - 장점: 특정 워크로드에 대해 2-3배 더 빠를 수 있음
  - 단점: 메모리에 두 개의 모델이 필요함
- KV 캐시 양자화: 메모리 사용량 감소 (int8, fp8, nf4)
- KV 캐시 퇴거 정책: LRU, LFU, 토큰 중요도
- 메모리 단편화: 낭비되는 VRAM을 최소화하는 방법

**실습 활동:**
- 수동으로 접두사 캐싱 구현:
  - 10개의 동일한 프롬프트에 대한 응답을 순차적으로 생성
  - 측정: 첫 번째 요청 vs 이후 요청의 지연 시간
  - SGLang이 프롬프트를 캐싱하여 이후 요청이 더 빨라져야 함
- 추측 해독 테스트 (SGLang이 지원하거나 대안 구현이 있는 경우):
  - 작은 초안 모델(예: Phi-2) + 큰 대상 모델(예: Mistral-7B) 사용
  - 단일 모델 베이스라인 대비 속도 향상 측정
- KV 캐시 양자화 실험 (가능한 경우):
  - 메모리 절약량 측정
  - 정확도 영향 측정 (출력 비교, 테스트 세트에서 퍼플렉서티 측정)
- KV 캐시 퇴거 스트레스 테스트:
  - 긴 문맥의 요청을 많이 전송
  - 어떤 요청이 퇴거되는지 관찰
  - 꼬리 지연 시간(tail latency, p99, p95) 측정
- **결과물:** 접두사 캐싱 및 추측 해독 분석 + 비교 보고서

**필요 리소스:**
- RunPod 인스턴스
- 40-50분의 실행 시간
- 잠재적으로 2개의 모델을 동시에 로드 (VRAM 확인)

---

### **4주차: 고급 주제 및 사이드 프로젝트**

#### **세션 7: 성능 최적화 및 벤치마킹**
**소요 시간:** 2시간 (학습 + 실습)  
**주요 개념:**
- 프로파일링 도구: PyTorch 프로파일러, NSys, 사용자 정의 타이밍 훅
- 병목 현상 식별: 시간이 어디에 소요되는가? (데이터 로딩, 토큰화, 연산, 통신)
- 지연 시간 분석: 토큰화 오버헤드, 첫 토큰 지연 시간, 토큰 간 지연 시간, 후처리
- 처리량 최적화: 최적의 배치 크기, 메모리 활용
- 벤치마킹 방법론: 일관되고 재현 가능하며 대표적인 워크로드
- MLPerf Inference 및 기타 표준화된 벤치마크
- 비용-성능 분석: 달러당 토큰 수, 지연 시간/비용 트레이드오프

**실습 활동:**
- 전체 추론 파이프라인의 엔드 투 엔드 프로파일링:
  - 토큰화 시간
  - 모델 순전파
  - 토큰 샘플링
  - 역토큰화(Detokenization)
  - 네트워크 오버헤드 (REST API 사용 시)
- 사용자 정의 벤치마크 스크립트 생성:
  - 모델 크기, 배치 크기, 문맥 길이, 생성 길이 변경
  - 각 구성에 대해 다음을 측정: 처리량, 지연 시간(p50, p95, p99), 메모리
  - 결과를 CSV로 내보내기
- SGLang과 다른 추론 엔진(vLLM, Ollama 또는 transformers) 비교:
  - 동일한 하드웨어, 동일한 모델, 동일한 워크로드
  - 비교 차트 생성
- RunPod에서 백만 토큰당 비용 계산:
  - 인스턴스 비용, 실행 시간, 생성된 토큰 수 고려
  - 어떤 구성이 가장 비용 효율적인가?
- **결과물:** 종합적인 벤치마크 결과 + 최적화 권장 사항

**필요 리소스:**
- RunPod 인스턴스
- 50-60분의 실행 시간 (더 긴 벤치마크 실행)
- 여러 모델 테스트 필요 가능성

---

#### **세션 8: 사이드 프로젝트 구축**
**소요 시간:** 2-3시간 (계획, 구현 및 발표 준비)  
**주요 개념:**
- 세션 1-7의 모든 개념 적용
- 의미 있는 결과물 구축: 실제 문제를 해결하거나 실제 질문에 답하기
- 프로젝트 아이디어 (하나를 선택하거나 직접 제안):
  
  **옵션 A: 특화된 추론 서버**
  - 도메인 특화 서빙 시스템 구축 (예: 구문 확인이 포함된 코드 생성 서비스, 언어 감지가 포함된 다국어 번역기, 또는 길이 제한이 있는 요약 서비스)
  - SGLang 백엔드 통합
  - 구조화된 생성 배포 (프롬프트, 제약 조건, 후처리)
  - 성능을 측정하고 가치 입증
  
  **옵션 B: 추론 최적화 툴킷**
  - 다양한 배치 크기, 시퀀스 길이, 하드웨어에서 여러 모델 프로파일링
  - 주어진 지연 시간/처리량 SLA에 대한 최적의 구성을 추천하는 도구 생성
  - 최적화 트레이드오프 시각화
  - RunPod 인스턴스에서 테스트
  
  **옵션 C: 멀티 모델 라우터 / 로드 밸런서**
  - 다음을 기준으로 서로 다른 모델에 요청을 라우팅하는 시스템 구현:
    - 프롬프트 특성 (길이, 도메인)
    - 지연 시간 요구 사항
    - 비용 제약
  - 정확도 및 효율성 향상 측정
  
  **옵션 D: 추론 비용 분석기**
  - 다음 항목에 걸쳐 실제 추론 비용을 측정하는 도구 구축:
    - 모델 (크기, 양자화 수준)
    - 샘플링 전략
    - 하드웨어 (다양한 RunPod 인스턴스 유형)
  - 대시보드 및 권장 사항 생성
  
  **옵션 E: 나만의 아이디어**
  - 본인의 관심사와 추론 엔진 개념을 결합한 프로젝트 제안
  - 예: RAG 시스템 최적화, 캐싱이 적용된 챗봇, 실시간 전사+추론 등

**실습 활동:**
- 4주차 1일 (세션 7 종료 후): 멘토의 피드백을 통해 프로젝트 아이디어 확정
- 핵심 기능 구현:
  - SGLang을 추론 백본으로 사용
  - 세션 1-7의 최적화 기법 중 3개 이상 적용
  - RunPod에서 철저히 테스트
- 벤치마킹 및 영향 측정:
  - 전/후 비교
  - 비용 분석
  - 성능 지표
- 발표 자료 작성:
  - 문제 정의
  - 아키텍처 다이어그램
  - 결과 및 발견 사항
  - 코드 저장소 (GitHub 또는 공유)
  - 라이브 데모 또는 녹화된 워크스루 (해당하는 경우)
- **결과물:** 완성된 프로젝트 저장소 + 프레젠테이션 (슬라이드 + 데모)

**필요 리소스:**
- RunPod 인스턴스 (여러 세션이 필요할 수 있음)
- 일주일간 총 약 2-3시간의 실행 시간
- 코드 저장소를 위한 GitHub 계정

---

## 사전 요구 사항

**필수 지식:**
- Python 능숙도 (스크립트 작성, PyTorch, NumPy 등의 라이브러리 사용)
- 신경망에 대한 기본 이해: 순전파, 역전파, 레이어
- 트랜스포머 아키텍처에 대한 친숙함 (어텐션, 임베딩, 피드포워드)
  - 깊은 수학적 지식은 필요하지 않으나, 개념적인 작동 방식을 이해해야 함
- 기본적인 Linux/Unix 명령줄 기술
- GPU 기초 이해: VRAM, CUDA, GPU 활용도

**우대 사항 (Nice-to-Have):**
- Hugging Face Transformers 라이브러리 사용 경험
- REST API 및 Python `requests`/`asyncio`에 대한 친숙함
- 기본적인 프로파일링 및 디버깅 기술
- Git 및 GitHub 기초 (사이드 프로젝트용)

---

## 추천 대상

**이상적인 후보자:**
- 대규모로 LLM을 배포하고자 하며 서비스 이면의 시스템을 이해해야 하는 **ML 엔지니어**
- 고성능 시스템, 최적화 및 인프라에 관심이 있는 **백엔드/시스템 엔지니어**
- 추론 효율성, 양자화 또는 새로운 서빙 기법을 연구하는 **연구원**
- LLM 기반 제품을 구축할 계획이며 비용/성능 트레이드오프를 이해해야 하는 **스타트업 창업자**
- 특정 주제에 대해 깊이 있게 파고들고자 하는 탄탄한 프로그래밍 기초를 갖춘 **학생/독학자**
- 프로덕션 환경에서 LLM 인프라 관리를 담당하는 **DevOps/MLOps 엔지니어**
- 이론 위주의 과정보다 실습 중심의 실험을 선호하는 **경쟁력 있는 학습자**

**최소 자격 요건:**
- 1년 이상의 Python 프로그래밍 경험
- 명령줄 도구 및 기본 Linux 환경에 익숙함
- 독립적으로 코드를 읽고 문제를 디버깅하려는 의지

---

## 비추천 대상

**권장하지 않는 대상:**
- **프로그래밍 완전 초보자** — 독립적으로 Python 코드를 작성하고 디버깅할 수 있다고 가정합니다. 먼저 Python 기초부터 시작하세요.
- **비기술직 관리자/기획자** — 이 과정은 "LLM이 작동하는 방식"에 대한 고수준의 과정이 아닙니다. 깊이 있는 기술적 콘텐츠입니다.
- **GPU 접근 권한이 없는 사람** — 실제 GPU에서 코드를 실행하지 않고는 추론 최적화를 효과적으로 배울 수 없습니다. CPU 전용 학습으로는 불충분합니다.
- **빠르고 얕은 지식을 구하는 사람** — 4주에 걸친 지속적인 노력이 필요합니다. 주말 단기 과정을 원한다면 이 과정은 적합하지 않습니다.
- **실습 없이 이론만 선호하는 학습자** — 이 과정은 실습과 실험 위주입니다. 논문만으로 추론을 배우고 싶다면 실습 세션에서 어려움을 겪을 것입니다.
- **동기 부여가 부족한 학습자** — 마지막의 사이드 프로젝트는 실제 작업과 창의성이 필요합니다. 이것은 수동적인 과정이 아닙니다.

---

## 성공 기준

이 프로그램이 끝날 무렵, 여러분은 다음을 수행할 수 있어야 합니다:
- [ ] SGLang이 요청을 스케줄링하고 배치 처리하는 방법을 설명할 수 있다.
- [ ] 추론 워크로드를 프로파일링하고 병목 현상을 식별할 수 있다.
- [ ] 캐싱, 배치 처리 및 양자화와 같은 기법을 통해 모델의 추론 성능을 20-50% 최적화할 수 있다.
- [ ] SGLang 서버를 배포하고 대안과 비교하여 벤치마킹할 수 있다.
- [ ] 추론 최적화 개념을 적용한 사이드 프로젝트를 완료하고 발표할 수 있다.
- [ ] 배운 내용과 코드에 대한 명확한 문서를 작성할 수 있다.

---

---

**행운을 빕니다! 이 과정은 도전적이지만 보람 있는 내용입니다. 과정을 마치면 현대적인 LLM이 어떻게 서비스되는지 이해하고, 스스로 최적화할 수 있게 될 것입니다.**
